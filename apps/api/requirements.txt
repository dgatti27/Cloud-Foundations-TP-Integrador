# Dependencias runtime de la Lambda (también vendorizadas en vendor/ para el zip).
# Rebuild vendor (desde apps/api):
#   pip install -r requirements.txt -t vendor --upgrade
pg8000>=1.31
scramp>=1.4.5
