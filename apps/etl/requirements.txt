# Dependencias de los ETL (completar segun los conectores reales)
psycopg2-binary>=2.9      # PostgreSQL (cruda + DW)
pymongo>=4.6              # Ecommerce y Eventos (MongoDB)
requests>=2.31            # repositorio de scraping (si expone HTTP)
boto3>=1.34               # Secrets Manager en AWS real
# dbf>=0.99               # tablas FoxPro (evaluar segun formato .dbf)
